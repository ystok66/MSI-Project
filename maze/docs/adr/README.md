# ADR

这个目录放 Architecture Decision Record。

建议只记录真正会长期影响结构的决定，例如：

- 为什么先做 `WARNING` 再做 `WAYPOINT`
- 为什么保留 `env / learner / tutor / runner` 分层
- 为什么 eval 必须区分 same-map 和 new-map

还没有形成稳定结论的内容，先写到 `docs/notes/`。
